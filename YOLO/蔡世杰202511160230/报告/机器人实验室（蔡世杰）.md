# 项目一报告

# Level1:配置YOLO环境

1.安装配置anaconda

*搜索清华大学开源软件镜像站下载anaconda

*添加环境变量

D:\Anaconda

D:\Anaconda\Scripts

*验证

Win+R到cmd

conda--version

conda info

2.创建独立虚拟环境

*从anaconda prompt里面写代码，创建是conda create -n 名称，激活是conda activate 名称、

*遇到问题：一开始不懂环境的重要性，创建了四五个新环境，在不同环境里下载安装了不同东西

*解决办法：从anaconda prompt里面写conda env list，回车，显示我所创建的所有环境，然后conda list 名称，即可看在不同环境里下载过的东西

3.判断无NVIDIA显卡，安装 CPU 版 PyTorch 及 YOLO 依赖

*打开设备管理器查看是否有nvidia显卡，发现自身电脑是intel显卡，所以下载cpu版的pytorch

*从官网搜索pytorch，下滑找到带有cpu only 的下载安装代码，将其在环境中粘贴安装

*从anaconda prompt中写python，复制yolo task=detect mode=predict model=yolov8n.pt source=https://ultralytics.com/images/zidane.jpg代码，即配置成功YOLO v8环境

可以从文件夹中找到所用的图片，同时检验

# Level2:下载和标注数据集

1.下载CCPD数据集

2.转换标注为YOLO .txt格式

*安装OpenCV

pip install opencv-python tqdm

pip install opencv-python matplotlib pillow -i https://pypi.tuna.tsinghua.edu.cn/simple

*检验安装

pip install opencv-python matplotlib pillow -i https://pypi.tuna.tsinghua.edu.cn/simple

*新建labels文件夹

*打开anaconda prompt激活环境

*新建记事本粘贴以下内容，然后打开pycharm

import os
import shutil
import random
from pathlib import Path
import cv2
import numpy as np

class CCPDProcessor:
    def __init__(self, dataset_path, output_path):
        self.dataset_path = Path(dataset_path)
        self.output_path = Path(output_path)
        self.output_path.mkdir(parents=True, exist_ok=True)
        
    def process_ccpd_dataset(self):
        """处理CCPD数据集"""
        print("开始处理CCPD数据集...")
        
        # 创建目录结构
        directories = ['images/train', 'images/val', 'images/test',
                      'labels/train', 'labels/val', 'labels/test']
        
        for dir_path in directories:
            (self.output_path / dir_path).mkdir(parents=True, exist_ok=True)
        
        # 获取所有图片文件
        image_files = list(self.dataset_path.glob('*.jpg'))
        random.shuffle(image_files)
        
        # 划分数据集 (70%训练, 20%验证, 10%测试)
        total = len(image_files)
        train_end = int(total * 0.7)
        val_end = int(total * 0.9)
        
        train_files = image_files[:train_end]
        val_files = image_files[train_end:val_end]
        test_files = image_files[val_end:]
        
        # 处理各个子集
        self._process_subset(train_files, 'train')
        self._process_subset(val_files, 'val')
        self._process_subset(test_files, 'test')
        
        print("数据集处理完成！")
    
    def _process_subset(self, files, subset):
        """处理子集数据"""
        for img_path in files:
            # 解析CCPD文件名格式获取标注信息
            # CCPD文件名格式: 025-95_113-154&383_386&473-386&473_177&454_154&383_363&402-0_0_22_27_27_33_16-37-15.jpg
            filename = img_path.stem
            parts = filename.split('-')
            
            if len(parts) >= 4:
                # 提取边界框信息
                bbox_info = parts[2]
                bbox_coords = bbox_info.split('_')
                
                if len(bbox_coords) >= 2:
                    # 转换坐标格式
                    try:
                        # 读取图像获取尺寸
                        img = cv2.imread(str(img_path))
                        if img is None:
                            continue
                            
                        h, w = img.shape[:2]
                        
                        # 解析边界框坐标（CCPD使用左上右下格式）
                        coords = []
                        for coord_pair in bbox_coords:
                            x, y = map(int, coord_pair.split('&'))
                            coords.extend([x, y])
                        
                        if len(coords) >= 4:
                            # 转换为YOLO格式 (中心点坐标和宽高，归一化)
                            x_min, y_min, x_max, y_max = coords
                            
                            x_center = (x_min + x_max) / 2 / w
                            y_center = (y_min + y_max) / 2 / h
                            width = (x_max - x_min) / w
                            height = (y_max - y_min) / h
                            
                            # 创建标注文件
                            label_path = self.output_path / 'labels' / subset / f"{img_path.stem}.txt"
                            with open(label_path, 'w') as f:
                                f.write(f"0 {x_center:.6f} {y_center:.6f} {width:.6f} {height:.6f}\n")
                            
                            # 复制图像文件
                            dest_img_path = self.output_path / 'images' / subset / img_path.name
                            shutil.copy2(img_path, dest_img_path)
                            
                    except Exception as e:
                        print(f"处理文件 {img_path.name} 时出错: {e}")
                        continue

# 使用示例
if __name__ == "__main__":
    processor = CCPDProcessor(
        dataset_path="D:\yolo\yolo\train\dataset\mydata\dataset\images\train",  # 替换为您的CCPD数据集路径
        output_path="D:\yolo\yolo\train\dataset\mydata\dataset\labels\train"    # 替换为输出路径
    )
    processor.process_ccpd_dataset()  

# Level3:划分数据集，配置并训练模型

1.划分数据集

*使用记事本输入代码后保存，打开pycharm打开记事本

```
import os
import shutil
import random
from pathlib import Path
from tqdm import tqdm
```

```
def split_dataset(source_img_dir, source_lbl_dir, output_dir, split_ratio=0.8):
    """
    将源数据集随机划分为训练集和验证集，并移动到新的目录结构中。

    参数:
    - source_img_dir (Path): 存放所有源图片的文件夹路径。
    - source_lbl_dir (Path): 存放所有源标签的文件夹路径。
    - output_dir (Path): 用于存放划分后数据集的新文件夹的根路径。
    - split_ratio (float): 训练集所占的比例。
    """
    
    # 1. 创建输出目录结构
    train_images_path = output_dir / "images" / "train"
    val_images_path = output_dir / "images" / "val"
    train_labels_path = output_dir / "labels" / "train"
    val_labels_path = output_dir / "labels" / "val"
    
    print("正在创建输出目录...")
    for path in [train_images_path, val_images_path, train_labels_path, val_labels_path]:
        path.mkdir(parents=True, exist_ok=True)
    print("目录创建完成！")

    # 2. 获取所有图片文件列表
    image_files = sorted([p for p in source_img_dir.glob("*.jpg")])
    
    if not image_files:
        print(f"警告: 在目录 '{source_img_dir}' 中未找到任何 .jpg 文件。脚本将退出。")
        return

    # 3. 随机打乱文件列表
    random.seed(42)  # 设置随机种子以保证每次划分结果一致
    random.shuffle(image_files)
    
    # 4. 计算划分点
    split_point = int(len(image_files) * split_ratio)
    train_files = image_files[:split_point]
    val_files = image_files[split_point:]
    
    print(f"总共找到 {len(image_files)} 张图片。")
    print(f"划分为: {len(train_files)} 张训练图片, {len(val_files)} 张验证图片。")
    
    # 5. 移动文件
    print("\n正在移动训练集文件...")
    for image_path in tqdm(train_files, desc="移动训练集"):
        label_path = source_lbl_dir / (image_path.stem + ".txt")
        if label_path.exists():
            shutil.move(str(image_path), str(train_images_path))
            shutil.move(str(label_path), str(train_labels_path))
        else:
            print(f"警告: 找不到图片 '{image_path.name}' 对应的标签文件，已跳过。")
            
    print("\n正在移动验证集文件...")
    for image_path in tqdm(val_files, desc="移动验证集"):
        label_path = source_lbl_dir / (image_path.stem + ".txt")
        if label_path.exists():
            shutil.move(str(image_path), str(val_images_path))
            shutil.move(str(label_path), str(val_labels_path))
        else:
            print(f"警告: 找不到图片 '{image_path.name}' 对应的标签文件，已跳过。")

    print("\n数据集划分完成！")
    print(f"新的数据集已保存在: {output_dir}")

if __name__ == '__main__':
    # --- 配置区域 ---
    # 请根据您的实际情况修改以下路径
    
    # 1. 包含所有原始图片的文件夹 (ccpd_parser.py脚本的SOURCE_IMAGES_DIR)
    SOURCE_IMAGES_DIR = Path(r"D:\yolo\yolo\train\dataset\mydata\images\train")
    
    # 2. 包含所有原始标签的文件夹 (ccpd_parser.py脚本的OUTPUT_LABELS_DIR)
    SOURCE_LABELS_DIR = Path(r"D:\yolo\yolo\train\dataset\mydata\labels\train")
    
    # 3. 您希望创建并存放最终划分好的数据集的新文件夹
    #    建议使用 'dataset' 或 'License_Plate_Dataset' 等名称
    OUTPUT_DATASET_DIR = Path(r"D:\yolo\yolo\train\dataset\mydata\dataset")
    
    # 4. 训练集所占的比例
    TRAIN_RATIO = 0.8
    # --- 配置结束 ---
    
    # 运行主函数
    split_dataset(SOURCE_IMAGES_DIR, SOURCE_LABELS_DIR, OUTPUT_DATASET_DIR, TRAIN_RATIO)
```

2.创建mydata.yaml配置文件

3.训练模型

*(chuangjian) D:\yolo\yolo\train\dataset\mydata\dataset>yolo train data=mydata.yaml model=yolov8n.pt epochs=100 imgsz=640 batch=64

# Level4：模型推理

1.对本地图片进行推理

*在pycharm中新建并输入

from ultralytics import YOLO
import cv2 

model = YOLO(r'D:\yolo\yolo\train\dataset\mydata\dataset\runs\detect\train10\weights\best.pt') 

image_path = r'D:\yolo\yolo\test\images'

results = model.predict(source=image_path, save=True)

print("推理完成！请检查 runs/detect/predict 文件夹下的结果。")

*然后运行得到新文件夹predict

![image-20251004170034805](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251004170034805.png)

2.对视频进行推理

*继续在pycharm里输入

from ultralytics import YOLO
import cv2 

model = YOLO(r'D:\yolo\yolo\train\dataset\mydata\dataset\runs\detect\train10\weights\best.pt')

video_path =r 'D:\yolo\yolo\test\video'

results = model.predict(source=video_path, save=True)

print("视频推理完成！请检查 runs/detect/predict 文件夹下的结果。")

*然后运行

![image-20251004170435308](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251004170435308.png)

3.对摄像头进行推理

*在pycharm里继续输入

from ultralytics import YOLO
import cv2 

model = YOLO(r'D:\yolo\yolo\train\dataset\mydata\dataset\runs\detect\train10\weights\best.pt')

results = model.predict(source="0", show=True, stream=True)

for r in results:

pass

*运行得到弹出的摄像头视频

![image-20251004170659394](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251004170659394.png)

